# features/

Feature modules live here, one folder per bettor feature (auth, home, event,
bet-history, account-statement, profit-loss, wallet, banks, …). Each feature owns
its components, hooks, API calls and types. Built incrementally per
`MIGRATION_PLAN.md` (Phases 1–5). Empty for now — added during migration.
